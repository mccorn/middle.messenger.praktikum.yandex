import { expect } from "chai";

describe("Router tests", () => {
	it("go", () => {
    expect(5).to.be.equal(5)
  });

	it("getRoute", () => {
    expect(5).to.be.equal(5)
  });

	it("use", () => {
    expect(5).to.be.equal(5)
  });
});
