export type SampleInput = {
	value: string, 
	placeholder?: string, 
	onBlur: FunctionStringCallback, 
}

export type SampleInputWithLabel = {
	value: string, 
	placeholder?: string, 
	onBlur: FunctionStringCallback, 
	label: string
}
